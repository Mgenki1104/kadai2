
  <main>
        <p id="notice"></p>
        <div class="container px-5 px-sm-0">

<div><%= flash[:notice] %></div>
<div class="row">
    <div class="col-md-3">
   <%= render 'users/new' %>

<div class="row">
  
  <%= link_to '', edit_user_path(@book.user), class: "btn btn-outline-secondary btn-block fas fa-user-cog" %>

</div>

<div class='A'><%= render 'books/new', book: @book %></div>

</div>
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table table-hover table-inverse">
  <thead>

  </thead>
  <tbody>

     <%# @books.each do |book| %>
      <tr>
        <td><%= link_to attachment_image_tag(@book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"), user_path(@book.user) %>
        <%= link_to @book.user.name, user_path(@book.user) %></td>
        <td><%= link_to @book.title, book_path(@book.id) %></td>
        <td><%= @book.body %></td>
      <% if @book.user == current_user %>

        <td><%= link_to "Edit", edit_book_path(@book), class: "btn btn-sm btn-success edit_book_10782" %></td>
        <td><%= link_to "Destroy", book_path(@book), method: :delete, data: {confirm: "本当に消しますか？"}, class: "btn btn-sm btn-danger destroy_book_10782" %></td>
      <% end %>
      </tr>
     <%# end %>
  </tbody>
</table>
  </div>
</div>
</div>
      </main>

